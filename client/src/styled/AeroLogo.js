import styled from "styled-components";

/**
 * @date 2020-02-05
 * @author MJ Kochuk
 * @module Styled
 */

/**
 * Used for holding the AeroMag 2000 logo in the header / navigation bar.
 **/
const AeroLogo = styled.img`
    height: 60px;
    `;

export default AeroLogo;